from django.apps import AppConfig


class PayloadSentinelConfig(AppConfig):
    """Configuration Django pour le package payload-sentinel."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "payload_sentinel"
    verbose_name = "Payload Sentinel"
