from django.apps import AppConfig


class RagConfig(AppConfig):
    """Configuration Django pour le package rag."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "rag"
    verbose_name = "Rag"